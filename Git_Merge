Git Merge is used to merge the two different branch commits based on chronologial order. Means it will merge the commits of 2 branches based on the timestamp the commit is created

command: git merge origin <branchname>
